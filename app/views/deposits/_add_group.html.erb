<%= form_for @grouped_deposit do |f| %>
  <%= f.hidden_field :group_id, value: group.id %>
  <%= f.hidden_field :group_id, value: @deposit.id %>
  <%= f.submit "Add to #{group.name}", class: 'w-100 new-bg-green text-white rounded-top border-none fs-5'  %>
<% end %>